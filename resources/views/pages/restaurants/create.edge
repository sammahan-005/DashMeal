@layout('layouts/main')


<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
      
      <div class="text-center mb-5">
        <div class="d-inline-flex align-items-center justify-content-center bg-light rounded-circle mb-3 shadow-sm" style="width: 80px; height: 80px;">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#E31E24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                <polyline points="9 22 9 12 15 12 15 22"></polyline>
            </svg>
        </div>
        <h2 class="fw-bold tracking-tight">Devenez Partenaire</h2>
        <p class="text-muted small">Enregistrez votre établissement sur DashMeal</p>
      </div>

      <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
        <div class="card-body p-4 p-md-5">
          
          <form action="{{ route('restaurants.store') }}" method="POST">
            {{ csrfField() }}

            <div class="mb-4">
              <label for="name" class="form-label small fw-bold text-uppercase">Nom de l'établissement</label>
              <input type="text" 
                     name="name" 
                     id="name" 
                     class="form-control form-control-lg rounded-3 border-light bg-light shadow-none fs-6 {{ flashMessages.has('errors.name') ? 'is-invalid' : '' }}" 
                     placeholder="Ex: La Marmite d'Or"
                     value="{{ old('name') || '' }}"
                     required>
              @if(flashMessages.has('errors.name'))
                <div class="invalid-feedback">{{ flashMessages.get('errors.name') }}</div>
              @endif
            </div>

            <div class="mb-4">
              <label for="description" class="form-label small fw-bold text-uppercase">Description</label>
              <textarea name="description" 
                        id="description" 
                        rows="3"
                        class="form-control rounded-3 border-light bg-light shadow-none fs-6 {{ flashMessages.has('errors.description') ? 'is-invalid' : '' }}" 
                        placeholder="Parlez-nous de vos spécialités (ex: Meilleures grillades de Yaoundé...)"
                        required>{{ old('description') || '' }}</textarea>
              @if(flashMessages.has('errors.description'))
                <div class="invalid-feedback">{{ flashMessages.get('errors.description') }}</div>
              @endif
            </div>

            <div class="mb-4">
              <label for="address" class="form-label small fw-bold text-uppercase">Localisation / Quartier</label>
              <div class="input-group">
                <span class="input-group-text bg-light border-light rounded-start-3">
                  <i class="bi bi-geo-alt text-muted"></i>
                </span>
                <input type="text" 
                       name="address" 
                       id="address" 
                       class="form-control form-control-lg rounded-end-3 border-light bg-light shadow-none fs-6 {{ flashMessages.has('errors.address') ? 'is-invalid' : '' }}" 
                       placeholder="Ex: Bastos, Rue de l'Ambassade"
                       value="{{ old('address') || '' }}"
                       required>
              </div>
              @if(flashMessages.has('errors.address'))
                <div class="text-danger small mt-1">{{ flashMessages.get('errors.address') }}</div>
              @endif
            </div>

            <div class="mb-5">
              <label for="phone_number" class="form-label small fw-bold text-uppercase">Téléphone (WhatsApp)</label>
              <div class="input-group">
                <span class="input-group-text bg-light border-light rounded-start-3">
                  <span class="small fw-bold text-muted">+237</span>
                </span>
                <input type="tel" 
                       name="phone_number" 
                       id="phone_number" 
                       class="form-control form-control-lg rounded-end-3 border-light bg-light shadow-none fs-6 {{ flashMessages.has('errors.phone_number') ? 'is-invalid' : '' }}" 
                       placeholder="6xx xxx xxx"
                       value="{{ old('phone_number') || '' }}"
                       required>
              </div>
              <div class="form-text x-small">Utilisé pour vous notifier des nouvelles commandes.</div>
              @if(flashMessages.has('errors.phone_number'))
                <div class="text-danger small mt-1">{{ flashMessages.get('errors.phone_number') }}</div>
              @endif
            </div>

            <button type="submit" class="btn btn-lg w-100 rounded-pill fw-bold text-white shadow-danger-btn py-3 mb-3" style="background-color: #E31E24; border: none;">
              Créer mon restaurant
            </button>
            
            <a href="{{ route('welcome') }}" class="btn btn-link w-100 text-muted text-decoration-none small fw-bold">
              Annuler et revenir
            </a>

          </form>
        </div>
      </div>

    </div>
  </div>
</div>

<style>
  body { background-color: #f8f9fa; }
  .form-control:focus {
    background-color: #fff !important;
    border-color: #E31E24 !important;
    box-shadow: none;
  }
  textarea.form-control {
    resize: none; /* Empêche le redimensionnement manuel pour garder le design propre */
  }
  .shadow-danger-btn {
    transition: all 0.3s ease;
  }
  .shadow-danger-btn:hover {
    box-shadow: 0 10px 20px rgba(227, 30, 36, 0.3) !important;
    transform: translateY(-2px);
  }
  .x-small { font-size: 0.75rem; }
</style>
@end