@layout('layouts/main')


<div class="container py-5 min-vh-100">
  
  <nav class="mb-4">
    <a href="{{ route('commandes.index') }}" class="text-decoration-none text-muted small fw-bold">
      <i class="bi bi-arrow-left me-1"></i> Retour à l'historique
    </a>
  </nav>

  <div class="row g-4">
    <div class="col-lg-8">
      <div class="card border-0 shadow-sm rounded-4 mb-4">
        <div class="card-body p-4">
          <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-800 h4 mb-0">Détails de la commande</h2>
            <span class="badge rounded-pill {{ commande.validated ? 'bg-success' : 'bg-warning text-dark' }} px-3 py-2">
              {{ commande.validated ? 'Validée' : 'En attente' }}
            </span>
          </div>

          <div class="table-responsive">
            <table class="table table-borderless align-middle">
              <thead class="text-muted small text-uppercase">
                <tr>
                  <th>Article</th>
                  <th class="text-center">Qté</th>
                  <th class="text-end">Prix</th>
                </tr>
              </thead>
              <tbody>
                @each(item in commande.menus)
                <tr class="border-bottom-dashed">
                  <td class="py-3">
                    <div class="d-flex align-items-center gap-3">
                      <div class="rounded-3 overflow-hidden" style="width: 50px; height: 50px;">
                        <img src="{{ item.image ? 'storage/' + item.image : '/images/food-plate.avif' }}" class="w-100 h-100 object-fit-cover" alt="">
                      </div>
                      <span class="fw-bold text-dark">{{ item.name }}</span>
                    </div>
                  </td>
                  <td class="text-center fw-bold">1</td> {{-- Ajuster si tu as une colonne quantité en pivot --}}
                  <td class="text-end fw-800">{{ item.price }} F</td>
                </tr>
                @end
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <div class="card border-0 shadow-sm rounded-4">
        <div class="card-body p-4">
          <h5 class="fw-bold mb-3">Informations de livraison</h5>
          {{--<div class="row">
            <div class="col-md-6 mb-3 mb-md-0">
              <p class="text-muted small text-uppercase mb-1">Restaurant</p>
              <p class="fw-bold mb-0">{{ restaurant.name }}</p>
              <p class="small text-muted">{{ restaurant.address }}</p>
            </div>
            <div class="col-md-6">
              <p class="text-muted small text-uppercase mb-1">Client</p>
              <p class="fw-bold mb-0">{{ auth.user.fullName }}</p>
              <p class="small text-muted">{{ auth.user.email }}</p>
            </div>
          </div>--}}
        </div>
      </div>
    </div>

    <div class="col-lg-4">
      <div class="card border-0 shadow-sm rounded-4 bg-white sticky-top" style="top: 20px;">
        <div class="card-body p-4">
          <h5 class="fw-bold mb-4">Résumé du paiement</h5>
          
          <div class="d-flex justify-content-between mb-2">
            <span class="text-muted">Sous-total</span>
            <span class="fw-bold">{{ commande.menus.reduce((sum, m) => sum + m.price, 0) }} F</span>
          </div>
          <div class="d-flex justify-content-between mb-2">
            <span class="text-muted">Frais de livraison</span>
            <span class="text-success fw-bold">Gratuit</span>
          </div>
          <hr>
          <div class="d-flex justify-content-between align-items-center mb-4">
            <span class="fw-bold h5 mb-0">Total</span>
            <span class="fw-800 h4 mb-0 text-dash-red">{{ commande.menus.reduce((sum, m) => sum + m.price, 0) }} F</span>
          </div>

          @if(!commande.validated)
            <form action="{{ route('commandes.validate', { id: commande.id }) }}" method="POST">
              {{ csrfField() }}
              <button class="btn btn-success w-100 rounded-pill py-3 fw-bold shadow-sm mb-2">
                <i class="bi bi-wallet2 me-2"></i> Valider & Payer
              </button>
            </form>
          @else
            <button class="btn btn-outline-dark w-100 rounded-pill py-3 fw-bold mb-2" onclick="window.print()">
              <i class="bi bi-printer me-2"></i> Télécharger le reçu
            </button>
          @endif

          <p class="text-center x-small text-muted mt-3 mb-0">
            ID Commande : <span class="text-dark fw-bold">#DS-{{ commande.id }}</span><br>
            Passée le {{ commande.createdAt.toFormat('dd/MM/yyyy à HH:mm') }}
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .fw-800 { font-weight: 800; }
  .text-dash-red { color: #E31E24; }
  .x-small { font-size: 0.75rem; }
  .border-bottom-dashed { border-bottom: 1px dashed #dee2e6; }
  .object-fit-cover { object-fit: cover; }
  
  @media print {
    .btn, nav, .sticky-top { display: none !important; }
    .card { border: 1px solid #eee !important; box-shadow: none !important; }
  }
</style>
@end